<!DOCTYPE html>
<html lang="en-NZ">
    <head>
        {% include "components/head.html" %}
        {% if css %}<link rel="stylesheet" href="/assets/css/{{css}}.css">{% endif -%}
        {% if js %}<script src="/assets/js/{{js}}.js"></script>{% endif -%}
        {% if js2 %}<script src="/assets/js/{{js2}}.js"></script>{% endif -%}
        <script src="/assets/js/headings.js"></script>
    </head>

    <body>
        <header>{% include "components/header.liquid" %}</header>
        <main>{{ content }}</main>
        <footer>{% include "components/footer.html" %}</footer>
    </body>
</html>
